# @siavava/* packages resolve from GitHub Packages. NPM_TOKEN must be a
# PAT (classic) with read:packages — set locally and in the deploy env.
[install.scopes]
"@siavava" = { url = "https://npm.pkg.github.com/", token = "$NPM_TOKEN" }
