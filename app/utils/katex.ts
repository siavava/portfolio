/**
 * ## katex (shim)
 *
 * Re-exports the shared KaTeX helpers so they stay auto-imported. The
 * implementation lives in `@siavava/markdown-math`.
 */
export { KATEX_MACROS, renderTex } from "@siavava/markdown-math"
