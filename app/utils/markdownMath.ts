/**
 * ## markdownMath (shim)
 *
 * Re-exports the shared marked+KaTeX renderers so they stay auto-imported.
 * The implementation lives in `@siavava/markdown-math`.
 */
export { renderInlineMath, renderMarkdownMath } from "@siavava/markdown-math"
