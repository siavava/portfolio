/** Typed FFI implementations for `App.Transformers.Tikz.FigAudit`. */

/** JS `Number(token)` — the SVG path tokenizer's numeric read. */
export const jsNumberImpl: (token: string) => number = Number
