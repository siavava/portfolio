---
title: "Neural Networks & Word Embeddings"
date: 2024-02-09
tag: "Natural Language Processing"
repo: "https://github.com/lostflux/linguistics/tree/main/P4"
featured: false
tech:
  - "Python"
  - "Neural Networks"
  - "Word Embeddings"
summary: |-
  A PyTorch neural network for multi-class text classification, plus a look at
  the geometry of its word embeddings via cosine similarity and t-SNE.
references:
  - https://notes.amittai.studio/natural-language-processing/semantics/neural-language-models
  - https://notes.amittai.studio/natural-language-processing/semantics/vector-semantics-and-embeddings
  - https://notes.amittai.studio/natural-language-processing/semantics/static-word-embeddings
---

A feedforward neural network in [PyTorch][pytorch] for
multi-class classification, paired with an inspection of the word
embeddings it learns — measuring closeness with
[cosine similarity][cosine-similarity] and
projecting the high-dimensional space to a plane with
[t-SNE][t-distributed].

The classifier is a network of linear layers and nonlinearities, mapping an
input to class scores by alternating affine maps with an elementwise
nonlinearity. With one hidden layer,

$$
\mathbf{h} = \phi(W_1 \mathbf{x} + \mathbf{b}_1),
\qquad
\mathbf{z} = W_2 \mathbf{h} + \mathbf{b}_2,
$$

where $\phi$ is a nonlinearity such as ReLU. The nonlinearity is what
lets the network represent functions a single linear layer cannot;
stacked layers compose into richer decision boundaries.

The output scores $\mathbf{z}$ become a distribution over the $K$ classes
with softmax, and training minimizes
the cross-entropy against the true label:

$$
P(y = k \mid \mathbf{x}) = \frac{e^{z_k}}{\sum_{j=1}^{K} e^{z_j}},
\qquad
\mathcal{L} = -\log P(y = y^{\ast} \mid \mathbf{x}).
$$

PyTorch handles the gradients by
[backpropagation][backpropagation]; the
weights update by gradient descent over minibatches.

The softmax carries a cost. That denominator sums $e^{z_j}$ over every
class, which is cheap for a handful of labels but ruinous when the
"classes" are a whole vocabulary, as in the skip-gram objectives that
learn general-purpose embeddings. Negative sampling sidesteps the sum:
each real (word, context) pair is trained against a few randomly drawn
_negative_ pairs, and one giant normalization collapses into a handful of
binary present-or-absent decisions. The vectors that fall out carry the
same geometry, learned at a fraction of the cost.

Embeddings carry meaning as geometry. The first layer maps each word to a
dense vector, trained end to end. After training, related words sit
close together, and closeness is measured by the angle between vectors
rather than their length:

$$
\operatorname{sim}(\mathbf{u}, \mathbf{v}) = \frac{\mathbf{u} \cdot \mathbf{v}}{\lVert \mathbf{u} \rVert \, \lVert \mathbf{v} \rVert}.
$$

Cosine similarity ignores magnitude, so frequent and rare words compare
on equal footing. t-SNE then compresses the embedding space to two
dimensions for plotting, preserving local neighborhoods so clusters of
related words show up visually — a qualitative check that the model
learned structure and not noise.

$$
% caption: A 2-D projection of a learned embedding space. Related words fall into
% loose clusters, and a consistent offset between analogous pairs (the dashed
% parallelogram) is the geometry behind $\text{king} - \text{man} + \text{woman} \approx \text{queen}$.
\begin{tikzpicture}[>=stealth, font=\footnotesize,
  dot/.style={circle, draw=acc, fill=acc!8, minimum size=2.6mm, inner sep=0pt}]
  \definecolor{acc}{HTML}{2348F2}
  \draw[->, black!55] (0,0) -- (6.2,0) node[anchor=north east, black!55] {dim 1};
  \draw[->, black!55] (0,0) -- (0,4.2) node[anchor=north west, black!55] {dim 2};
  % nouns cluster
  \node[dot] at (4.5,3.0) {}; \node[dot] at (5.0,3.2) {};
  \node[dot] at (4.7,2.7) {}; \node[dot] at (5.1,2.9) {};
  \node[font=\ttfamily, text=black!70, anchor=south] at (4.8,3.55) {cat dog fish};
  % verbs cluster
  \node[dot] at (4.5,1.0) {}; \node[dot] at (5.0,1.2) {};
  \node[dot] at (4.7,0.8) {}; \node[dot] at (5.1,1.0) {};
  \node[font=\ttfamily, text=black!70, anchor=north] at (4.8,0.5) {run walk swim};
  % analogy parallelogram
  \coordinate (m) at (1.1,1.5); \coordinate (k) at (2.5,1.5);
  \coordinate (w) at (1.5,2.9); \coordinate (q) at (2.9,2.9);
  \draw[dashed, acc] (m) -- (k) -- (q) -- (w) -- cycle;
  \node[dot] at (m) {}; \node[dot] at (k) {};
  \node[dot] at (w) {}; \node[dot] at (q) {};
  \node[font=\scriptsize\ttfamily, text=acc, anchor=east] at (0.95,1.35) {man};
  \node[font=\scriptsize\ttfamily, text=acc, anchor=west] at (2.65,1.35) {king};
  \node[font=\scriptsize\ttfamily, text=acc, anchor=east] at (1.35,3.05) {woman};
  \node[font=\scriptsize\ttfamily, text=acc, anchor=west] at (3.05,3.05) {queen};
\end{tikzpicture}
$$

The clusters are the payoff: words with similar usage land near each
other, and the parallelogram shows that a relation like gender is a single
shared direction, so the space encodes structure, not just proximity.

You can read the
[full report][linguistics-00].

[pytorch]:           https://pytorch.org/
[cosine-similarity]: https://en.wikipedia.org/wiki/Cosine_similarity
[t-distributed]:     https://en.wikipedia.org/wiki/T-distributed_stochastic_neighbor_embedding
[backpropagation]:   https://en.wikipedia.org/wiki/Backpropagation
[linguistics-00]:    https://github.com/lostflux/linguistics/blob/main/P4/report/00.report.pdf
